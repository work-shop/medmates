{% extends "base.twig" %}

{% block content %}
  <h1 class="mb4 f2 fw6 lh-title green">{{ posts[0].type.labels.name }}</h1>

  {% for post in posts %}
    {% include ["includes/teaser-" ~ post.post_type ~ ".twig", "includes/teaser.twig"] %}
  {% endfor %}
{% endblock %}
