{% extends "base.twig" %}

{% block content %}
  <article class="measure-wide">
    {#<h1 class="mb3 f2 fw6 lh-title green">{{ post.title }}</h1>#}

    <div class="mb4 f6 fw6 ttu tracked gray">Posted {{ post.post_date_gmt | time_ago }} by {{ post.author }}</div>

    <div>{{ post.content }}</div>
  </article>
{% endblock %}
